const config = {
    tmdbapikey: String(import.meta.env.VITE_TMDB_KEY),
    tmdbRAT: String(import.meta.env.VITE_TMDB_RAT),
    tmdbURL: String(import.meta.env.VITE_TMDB_URL),
}

export default config;